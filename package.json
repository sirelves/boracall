{
  "name": "boracall",
  "version": "0.1.3",
  "private": true,
  "description": "BoraCall desktop — native shell over the web prototype",
  "scripts": {
    "tauri": "tauri",
    "dev": "tauri dev",
    "build": "tauri build"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2"
  }
}
